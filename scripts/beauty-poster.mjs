import { chromium } from '@playwright/test';
const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage({viewport:{width:1280,height:720}});await p.setContent('<video muted playsinline preload="auto" src="http://localhost:3110/videos/beauty-in/beauty-in-preparation-v1.mp4"></video>');await p.locator('video').evaluate(v=>new Promise(r=>v.readyState>=2?r():v.addEventListener('loadeddata',r,{once:true})));await p.locator('video').evaluate(v=>new Promise(r=>{v.addEventListener('seeked',r,{once:true});v.currentTime=v.duration*.94}));await p.screenshot({path:'public/videos/beauty-in/beauty-in-poster.png'});await b.close();
